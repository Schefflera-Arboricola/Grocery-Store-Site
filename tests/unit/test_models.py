from application.models import *

def test_customer_model():
    '''
    GIVEN a Customer model
    WHEN a new Customer is created
    THEN check the email, first_name, last_name, and password fields are defined correctly
    '''
    return True
    