Single-database configuration for Flask.

[
    NOTE : This directory was auto generated when the db structure was modified, using the following shell commands(in the virtual environment):

    1. `export FLASK_APP=main.py:app` and exporting other API keys
    2. `flask db init`
    3. `flask db migrate -m "Add isDeleted column to Products table"`
    4. `flask db upgrade`

]